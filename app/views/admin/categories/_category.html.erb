<tr>
  <td>
    <h4><%= category.name %></h4>
    <br>
  </td>
  <td>
    <%= category.products.length %>
  </td>
  <td>
    <%= link_to "Products in #{category.name}", category_path(category.id), class: "btn btn-primary" %>
  </td>
</tr>